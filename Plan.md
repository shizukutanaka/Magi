# Magi — 開発計画書

世界各地の占術を横断的に試せる、**完全無料**のWebアプリケーション。
コードはGitHubで公開する（MIT）。

---

## 0. 方法論

本計画は **ソクラテス式問答**（前提を問い直す）と **イーロン・マスク式思考法**
（第一原理思考 → 5ステップ・アルゴリズム）で導出する。実装は結論だけでなく、
「なぜその形なのか」を辿れる状態で残す。

---

## 1. ソクラテス式問答 — 前提の解体

**Q1. 利用者は「占い」に何を支払っているのか。当たる予言か？**
違う。当たり外れは検証不能で、検証されても継続の理由にならない。
支払われているのは **「意思決定と内省のための、外部から与えられる構造」** である。
毎日ひとつ、自分の外側から問いと象徴が渡され、それを解釈する時間が生まれる。

**Q2. ならば「当たること」は要件から外せるか？**
外せる。外すべきである。予言の的中を約束した瞬間、検証不能な主張の販売になり、
法的にも倫理的にも脆くなる。要件は「一貫性・再現性・解釈の豊かさ」に置き換わる。

**Q3. 一貫性とは何か。占いは乱数ではないのか？**
乱数だが、**同じ人が同じ日に同じ問いで引いた結果は同じでなければならない**。
朝引いたタロットが昼にリロードで変わるなら、それは占いではなくガチャである。
→ 乱数は「入力から決定的に導かれるシード」に置き換わる。
   `seed = SHA256(subject_key | engine_id | target_date | 正規化した問い | 生年月日 | 氏名 | 実効オプション)`
   実効オプションとは、流派が受け付けるキー（`default_options` のキー）だけを残し、
   省略されたキーを既定値で埋めた後のオプションである（Q20）。

**Q4. なぜ「世界の色々な占い」でなければならないのか。ひとつで足りないか？**
足りない。単一占術は「もう見た」で飽きる。一方、複数の伝統は同じ問いに
**別々の象徴体系で答える** ため、比較そのものが体験価値になる（西洋タロットの「塔」と
易経の「震為雷」を並べる）。多流派は装飾ではなく、**本プロダクトが存在する理由**そのものである。

**Q5. では、いくらで売るべきか？**
**売らない。** 当初は月額サブスクを前提に設計していたが、これは前提から誤っていた。
課金する以上、「支払った人だけが得られるもの」を作らねばならない。しかし本アプリの提供物
（第2章の通り限界費用ほぼゼロの純関数と静的テキスト）で差をつけようとすると、
**わざと劣化させた無料版を作る**しかない。占術を減らす、カードを減らす、履歴を消す——
いずれも「利用者のための設計」ではなく「課金のための設計」であり、プロダクトを歪める。

さらに課金は、課金それ自体を支えるための巨大な付随物を要求する：ユーザー登録、認証、
決済事業者との連携、Webhook、契約状態の同期、解約導線、返金、税務。
**占いのコードより課金のコードの方が大きくなる。** 限界費用がゼロの商品に対して、
これは明らかに不合理な投資である。

→ 完全無料で公開する。中核体験「今日の三賢者（Daily Magi）」は、
毎日ログインさせるための仕掛けではなく、単に毎日価値が生まれる形だから採用する。

**Q6. 生成AIに毎回文章を書かせるべきか？**
すべきでない。第一原理で見ると、LLM推論は 1リクエストあたり数円のコストと数秒の遅延、
そして **非決定性**（Q3の要件違反）を持ち込む。占術の解釈テキストは有限個の
象徴（78枚・64卦・24ルーン…）の組み合わせであり、**あらかじめ書ける**。
→ v1のコアは純関数。LLMは将来「解釈の言い換え」に限定した任意機能とする。

**Q6-b. 無料なら、コードを公開する意味はどこにあるのか？**
**信頼**である。占いアプリに向けられる最大の疑念は「結果は操作されているのではないか」
（課金導線に有利な結果が出る、広告のために不安を煽る）。無料かつコード公開かつ
決定的シード（Q3）という3点が揃って初めて、**利用者は自分の結果を自分で再現・検証できる**。
これは既存の占いアプリがほぼ提供できていない性質であり、本プロダクトの唯一の差別化である。

**Q7. 無料は持続可能か。誰がサーバ代を払うのか？**
ここが本当に解くべき問題である。しかし第2章の分解を見れば、**サーバに状態を持つ理由が
そもそも無い**ことがわかる。占術は純関数、解釈は静的データ、鑑定結果はその場で計算できる。
ユーザーごとの状態は「過去に何を引いたか」だけであり、それは**利用者自身のブラウザに置けばよい**。
→ データベースを持たない。ログインを持たない。個人情報を収集しない。
結果として運用費は静的配信＋軽量APIのみとなり、無料枠のホスティングで賄える。
誰かの善意にも広告にも依存しない。**費用がほぼゼロだから無料が持続する。**

**Q8. フロントエンドに専用サーバとビルド工程は必要か？**
必要ない。サーバ状態もSSRすべき動的コンテンツも無いため、専用サーバとビルド工程は
ユーザーに見える価値を増やさない。→ 静的ファイルをFastAPIから配信し、self-hostは
1プロセスのままにする。npm依存とビルド工程、2つ目のデプロイ面を削除できる。

**Q9. self-hostにcomposeとオーケストレーションは必要か？**
必要ない。プロセスは1つ、バッキングサービスは0なので、composeを追加しても
`docker run` と等価なファイルを増やすだけである。代わりに価値があるのは、
受け取った鑑定を第三者が手元で再現・検証できることだ。composeではなく、
結果の再現検証CLIを追加する。

**Q10. 流派は数を増やすべきか？**
増やすべきなのは数ではなく機構の種類である。既存8流派はすべて「集合から引く」
機構で、9番目に同型のカードデッキを足しても体験は増えない。ジオマンシーは
4本の偶奇線から生成し、加算で図形を導出する別機構なので追加した。

**Q11. 翻訳はシードに含めるべきか？**
含めない。言語は表示層であり、シードに入れると同じ共有URLが読む人の言語で
別の結果を返し、再現性という中核価値が壊れる。訳が無い流派は「無い」とAPIで
申告してフォールバックする方が、機械翻訳で埋めるより誠実である。

**Q12. 英訳はデータ行に `*_en` を足すべきか？**
足さない。ロケールを1つ増やすたびに全データ行を書き換えることになり、
日本語データが唯一のソースである性質も失われる。日本語データはそのまま残し、
安定IDをキーにした別ロケール層（`data/en/`）に訳を置き、翻訳対象キーは
データ行から導出する。導出したキー集合が英訳辞書に含まれているかを
テストで検査するので、訳し忘れは静かな日本語フォールバックではなく
テスト失敗として現れる。`interpretation_langs` も手書きの一覧ではなく
この網羅率から導出するため、訳を足すだけでAPIの申告が変わる。

**Q13. 問いを入力させて、解釈が問いに触れないのは許されるか？**
許されない。問いはシードを変えるだけで、9流派どれも解釈が問いに言及していなかった。
入力欄は「これを書けば結果が変わる」という約束であり、約束を守らない入力欄は
削除するか、守らせるかのどちらかしかない。占いにおける問いは中核の入力なので
削除せず、守らせる方を選ぶ。LLMは使わない（同じ問いが同じ結果を返さなくなり、
再現性という中核価値が壊れ、鍵と課金と外部依存が連鎖的に戻ってくる）。
代わりに問いをキーワード出現数で決定的に7領域（恋愛・仕事・金銭・健康・
人間関係・決断・全般）へ分類し、その領域に向けたセクションを総合の直後に1つ挿入する。
分類辞書は日英を同じ辞書に持つため、言語を変えても領域は変わらない。
問いが空なら挿入しない（問いの無い鑑定の出力は従来と一字も変わらない）。

**Q14. 使われていないフィールドを残しておく害はあるか？**
ある。`birth_time` は入力欄を出しておきながらシードにもエンジンにも一切使われて
いなかった。`image_hint` は全エンジンが返すのに画像は1枚も存在せずUIも読んでいなかった。
前者は利用者に無意味な個人情報を入力させ、後者はAPI利用者に存在しない機能を約束する。
最良の部品は存在しない部品なので両方削除した。画像を実装する日が来ても
`engine_id` と `key` から経路は再構成できるので、情報は失われていない。

**Q15. レートリミットは本当に機能していたか？**
していなかった。`X-Forwarded-For` を無条件に信頼していたため、ヘッダを1リクエスト
ごとに変えれば毎回別クライアントとして数えられた。「濫用防止のためにレートリミットだけは
残す」という判断は、偽装できるキーで数えている限り自己満足である。信頼できるのは
運用者が「この前段プロキシを信頼する」と明示した場合だけなので、`MAGI_TRUST_PROXY`
が真のときだけヘッダを見る。Uvicorn自身も既定で `--proxy-headers` により
`client.host` を書き換えるため、既定の起動コマンドでは `--no-proxy-headers` を指定し、
信頼の判断をアプリ側の1つのスイッチに集約する。

**Q16. 入力の長さに上限は必要か？**
必要。保存もアカウントも無いので巨大な入力で「溜まる」ものは無いが、
問いも氏名も無制限のまま受け取っていた。占いの問いは1〜2文で足りる
（`question` 200字、`full_name` 100字、`subject_key` とオプションは64字）。
上限はpydanticの型で表明し、超過は422として現れる。

**Q17. 決定性は「正しさ」より優先されるか？**
されない。易経は6爻の陰陽をそのまま二進数にして+1した値を卦番号として使っていたが、
二進の並び順はKing Wen序と一致しない。六爻すべて陽は乾為天（第1卦）であるべきところ
火水未済（第64卦）を返していた。**引いた爻と表示される卦が対応していない**のだから、
これは解釈の好みではなく機構の誤りである。過去の共有URLの再現性は中核価値だが、
「誤った結果を忠実に再現し続ける」ことは価値ではないので、対応表を入れて直した。
対応表は手書きの一覧を信じるのではなく、データ側の卦名（`水雷屯` なら上卦=水・下卦=雷）
から独立に導出した表と一致することをテストで検査する。爻の抽選（三枚の硬貨×6回）は
変えていないので、変わったのは「同じ爻から導かれる卦の同定」だけである。

**Q18. 簡略化していることを黙っていてよいか？**
よくない。天体暦を使わない占星術、年柱と日柱だけの四柱推命、コードポイントで
数値化する数秘術は、いずれも伝統的な算法とは違う。無料でOSSであることは
「中身が何であるかを説明しない」ことの許可ではないので、READMEに何を省いているかを
流派ごとに列挙する（例: 四柱推命の年の切り替わりは立春ではなく1月1日）。

**Q19. 「規約」と「誤り」はどこで分かれるか？**
外部の検証可能な基準と突き合わせられるかどうかで分かれる。マヤ暦のオフセット0は
「v1の規約」と書いてあったが、Long Count 13.0.0.0.0（2012-12-21）は 4 Ahau という
外部基準があり、オフセット0はそれを満たさない。伝統的ツォルキンともドリームスペルとも
一致しないのだから、これは規約ではなく誤りを言い替えたものである。Q17の易経と同じ問いを
当てて、相関日と 2012-12-21 が kin 160 = 4 Ahau になるようオフセットを 159 に直した。
逆に四柱推命は外部の暦と突き合わせて日柱も年柱の基点も一致したので、唯一の差異
（年の切り替わりが立春ではなく1月1日）だけが簡略化であり、これは規約として明示すればよい。

**Q20. 「APIが受け付ける入力」と「共有URLが運べる入力」がずれていてよいか？**
よくない。`options` は全部シードに入るのに、共有URLと検証CLIが運ぶのは `spread` だけだった。
したがって `options={"foo":"bar"}` のような入力は**誰も再現できない鑑定**として成立し、
易経に `spread=celtic-cross` を渡すことも通っていた。再現性が中核価値なら、
APIが受け付ける入力の集合は共有URLが運べる入力の集合と等しくなければならない。
→ 流派が受け付けるキーは `default_options` のキーと等しいという規則で実効入力を正規化し、
未知のキーは 422 で拒否する。シードは正規化後の実効オプションから作る。

**Q21. 数値のスコアは「あった方が親切」か？**
違う。9流派中8流派の `score` は `rng.randint` の乱数で、実測すると大凶の平均が大吉を上回っていた。
利用者は0-100の数字を「その鑑定の判定」と読むので、これは装飾ではなく虚偽である。
伝統自身が吉凶を格付けするのは、七段階を持つおみくじと、判定図が吉凶を持つジオマンシーだけ。
→ その2流派は等級から固定値を引き、残る7流派は `null` を返す。無いものは無いと言う。

**Q22. 象徴を引いておいて、その意味を言わないのは占いか？**
占いではない。ケルト十字は10枚引いて先頭1枚の意味しか使わず、残り9枚は名前だけ表示していた。
数秘術はライフパスが1でも8でも本文が同一で、マヤ暦は紋章・音の意味を1つも持たず、
四柱推命は干支の名前だけを出していた。「引いた象徴 × ポジション」に1対1で本文を対応させ、
象徴が変われば本文が変わることをテストで検査する。固定文のセクションは削除する
（本文が象徴に依存しない節は、存在しても情報を1ビットも運んでいない）。

**Q23. 共有URLに載せる識別子は何であるべきか？**
鑑定を再現するのに必要な最小の値だけである。ブラウザの永続IDをそのまま `s=` に載せていたため、
同じ人が共有した複数のリンクは同一ブラウザ由来だと外から結び付けられ、サーバのログだけで
そのブラウザの鑑定履歴を再構成できた。個人情報を保存しない設計は、URLで配ってよい理由にならない。
→ `s=` は鑑定ごとのトークン `SHA256(ブラウザの秘密鍵 + その鑑定の入力)` にする。
リンクは1本ずつ独立になり、再現性は保たれる。トークンが空や欠落なら「共有された鑑定ではない」と
明示する（黙って別の鑑定を出すのは、再現性の主張に対する裏切りである）。

**Q24. 履歴が記録すべき単位は「描画」か「鑑定」か？**
鑑定である。保存を描画のタイミングで呼んでいたため、言語を2回切り替えて履歴から開き直すだけで
同じ鑑定が4件並んだ。鑑定の同一性は `seed` で決まるのだから、同一性の鍵も `seed` にする。
あわせて実行中の連打を塞ぐ（同じ入力から複数の履歴が生まれる経路を残さない）。

**Q25. 静的ファイルにキャッシュ制御は要らないか？**
要る。バンドルもファイル名ハッシュも無い構成では、更新して再起動しても
ブラウザが古い `app.js` と新しい `store.js` を混ぜて実行しうる（開発中に実際に踏んだ）。
→ `Cache-Control: no-cache` を付け、ETag/Last-Modified による再検証を必ず通す。

**Q26. 「異なる伝統から3流派」を、表示文字列で判定してよいか？**
よくない。数秘術の表示だけが `西洋（ピタゴラス）` だったため、タロットと同時に
「別の伝統」として選ばれ得た。さらに全流派を平坦にシャッフルして先頭を取る実装は、
流派数の多い文化圏を選びやすく（西洋67.3% 対 中米40.9%）偏っていた。
翻訳される文字列は計算に使えないので、安定キー `culture` を導入し、
文化圏を等確率で3つ選んでから各文化圏内で1流派を等確率に選ぶ（実測49.1〜51.0%）。
**一般化**: 表示のための文字列が計算に混ざっている箇所は、すべて同じ種類のバグを抱えている。

**Q27. 「CIがある」とは、YAMLが存在することか？**
違う。実行されて初めてCIである。`ci/backend-ci.yml` は書いてあるが `.github/workflows/` に
無いため、これまでのPRのチェックは一貫して0件だった（決定性を中核価値に置く製品で、
決定性を守るテストが1本も自動実行されていない）。トークンに `workflow` scope が無く
自動配置できないので、**未完了として明示する**のが誠実な状態である
（「CIがある」と書いて済ませることは、Q21の乱数スコアと同種の虚偽になる）。
ワークフローの各ステップは手元で全て実行し、通ることを確認している。

---

## 2. 第一原理からの再構築

物理的に必要なものだけに分解する。

| 要素 | 物理的実体 | 限界コスト |
|---|---|---|
| 占術の計算 | 決定的な純関数（表引き＋シード付きシャッフル） | ≒ 0 |
| 解釈テキスト | 静的データ（一度書けば無限に再利用） | 0 |
| 保存 | 1鑑定あたり数KBのJSON | ≒ 0 |
| 配信 | HTTPレスポンス | ≒ 0 |

**結論**: 限界費用がほぼゼロである。ならば利用者を絞る技術的理由は存在しない。
ゼロコストの商品にわざわざ計量・課金・制限の機構を足すことは、**費用を増やして価値を減らす**行為である。
→ 全機能を無条件に開放する。

### マスク式5ステップの適用

1. **要件を疑う** — 「占星術には天体暦(ephemeris)が要る」→ v1のホロスコープは
   太陽星座・月相・曜日支配星までで成立する。数百MBの暦データは v1 の要件ではない。
2. **部品を削除する** — 削除するもの: 会員同士のSNS、チャット、鑑定士マーケット、
   広告、ネイティブアプリ、LLM常時利用、リアルタイム通知基盤。
3. **単純化・最適化** — 全占術を `cast(input, rng) -> Reading` という **単一インタフェース**に
   統一する。流派の追加は「1ファイル追加」で終わる形にする（これが本プロダクトの成長率を決める）。
4. **サイクルタイムの短縮** — 占術エンジンはDB・ネットワーク・時刻に依存させない。
   純関数ゆえにテストはミリ秒で終わり、1日に何十回でも流派を追加できる。
5. **自動化は最後** — デプロイやコンテンツ生成の自動化は、流派が揃ってから。

---

## 3. プロダクト定義

### 中核体験
**今日の三賢者**: 毎朝、異なる伝統から3つの占術が自動で選ばれ、同じ問いに答える。
3つの解釈の「重なり」をアプリが要約する。毎日、3流派を引ける。

無料・ログイン不要・DBなしの帰結として、**個人情報を一切保存しない**。
生年月日や氏名は鑑定の計算にのみ使い、サーバ側には残さない（ログにも出さない）。

### 収録占術（v1: 9流派・すべて無料）

| ID | 名称 | 文化圏キー | 表示上の伝統 | 必要入力 | 象徴数 |
|---|---|---|---|---|---|
| `tarot` | タロット | `western` | 西洋 | なし（問いは任意） | 78枚 × 正逆 |
| `iching` | 易経（周易） | `chinese` | 中国 | なし | 64卦 × 変爻 |
| `runes` | ルーン | `nordic` | 北欧 | なし | エルダー・フサルク24 |
| `omikuji` | おみくじ | `japanese` | 日本 | なし | 7段階＋7項目 |
| `astrology` | 西洋占星術 | `western` | 西洋 | 生年月日 | 12星座＋月相 |
| `numerology` | 数秘術 | `western` | 西洋（ピタゴラス） | 氏名・生年月日 | 1-9, 11, 22, 33 |
| `bazi` | 干支・四柱推命（略式） | `chinese` | 中国 | 生年月日 | 十干十二支60 |
| `mayan` | マヤ暦ツォルキン | `mesoamerican` | 中米 | 生年月日 | 260 kin |
| `geomancy` | ジオマンシー | `afro-european` | 西アフリカ・ヨーロッパ | なし | 16図形の盾形図 |

文化圏キーは計算に使う安定キー、表示上の伝統は翻訳される表示文字列である（Q26）。
スコアを返すのは、伝統自身が吉凶を格付けする `omikuji` と `geomancy` だけで、
残る7流派は `score` に `null` を返す（Q21）。

### 提供範囲

9流派すべて、全スプレッド、今日の三賢者、無制限に無料。ログイン不要。
利用制限は **濫用防止のためのIPレートリミットのみ**（プラン差ではない）。
履歴は利用者のブラウザ（localStorage）に保存し、エクスポート・削除は利用者の手にある。

---

## 4. アーキテクチャ

技術スタックは kAIkei と揃える（同一開発者の学習コストを増やさない）。

- Backend: Python 3.12 / FastAPI / Pydantic v2（**ステートレス。DBなし・認証なし**）
- Frontend: HTML / CSS / ES Modules（FastAPIから静的配信、履歴はlocalStorage）
- CI: GitHub Actions のワークフローを `ci/backend-ci.yml` に置く（Ruff・pytest・
  共有URLのオフライン検証CLI・フロントの翻訳網羅テスト）。`.github/workflows/` へ
  配置するまで**1本も実行されない**ので、有効化はリポジトリ管理者の作業として README に書く（Q27）
- 配信: 静的フロント＋軽量API。誰でも `docker run` で self-host できる形を維持する。

```
backend/app/
├── divination/            # 占術ドメイン（DB・HTTP非依存の純粋層）
│   ├── base.py            # DivinationInput / Reading / DivinationEngine
│   ├── seed.py            # 決定的シードと SeededRandom
│   ├── registry.py        # エンジン登録・取得
│   ├── engines/           # 1流派 = 1ファイル
│   └── data/              # 象徴と解釈テキスト（静的JSON/Python）
├── api/v1/                # HTTP境界（ステートレス）
└── core/                  # 設定・レートリミット
```

### 中核インタフェース

```python
class DivinationInput(BaseModel):
    target_date: date                 # 占う対象日（既定=今日）
    question: str | None = None       # 200字まで。シードに入り、領域分類にも使う
    birth_date: date | None = None
    full_name: str | None = None      # 100字まで
    options: dict[str, str] = {}      # spread など流派固有の指定

class DrawnSymbol(BaseModel):
    key: str                          # "the-tower" / "hex-51" / "fehu"
    name: str
    position: str                     # "過去" / "上卦" など
    reversed: bool = False

class Reading(BaseModel):
    engine_id: str
    engine_name: str
    tradition: str                    # 表示用（翻訳される）
    seed: str                         # 検証可能な形で必ず返す
    drawn: list[DrawnSymbol]
    summary: str
    sections: list[ReadingSection]    # 流派の機構に沿った節（例: タロットなら
                                      # 過去／問いについて／現在／未来／助言）
    score: int | None                 # 0-100。格付けを持つ流派のみ、他は None
    lucky: LuckyItems | None
    generated_at: datetime
    disclaimer: str
    lang: str                         # 表示言語
    interpretation_lang: str          # 解釈本文が実際に存在した言語

class DivinationEngine(Protocol):
    id: str; name: str; culture: str  # culture は計算に使う安定キー
    required_fields: frozenset[str]
    default_options: dict[str, str]   # 受け付けるオプションキーと既定値
    def cast(self, inp: DivinationInput, rng: SeededRandom, lang: Lang = "ja") -> Reading: ...
```

**不変条件**: `cast` は同一 (input, seed) に対して常に同一の `Reading` を返す。
これは全エンジン共通のテストで機械的に検証する。

---

## 5. ロードマップ

| フェーズ | 内容 | 状態 |
|---|---|---|
| P1 | 占術ドメイン層（8流派）＋ 決定的シード ＋ ステートレスAPI | 完了 |
| P2 | 静的フロントエンド（今日の三賢者・鑑定画面・履歴=localStorage・共有リンク） | 完了 |
| P3 | 流派拡張（ジオマンシーで9流派） | 完了（ホラリー占星術・カバラは未着手） |
| P4 | self-host 一式（Dockerfile・デプロイ手順）と結果の再現検証CLI | 完了 |
| P5 | 多言語対応（表示言語の機構と9流派すべての解釈本文の英訳） | 完了 |
| P6 | 問いを解釈に反映する決定的な領域分類（Q13） | 完了 |
| P7 | 機構の検算（易経のKing Wen序・マヤ暦の相関・占星術の月相・四柱推命の外部暦照合） | 完了 |
| P8 | 解釈の網羅（引いた全象徴に意味を与える）と捏造スコアの撤去 | 完了 |
| P9 | 共有・履歴・配信の健全化（識別子の分離・履歴の同一性・再検証ヘッダ） | 完了 |
| P10 | CI の実行（`.github/workflows/` への配置） | 未完了（管理者の作業、Q27） |

**削除した計画**: LLMによる解釈の言い換え。問いへの応答は決定的な領域分類で足り、
LLMを入れると同じ共有URLが同じ結果を返さなくなる（Q13）。

**削除した計画**: 認証、PostgreSQL、Stripe課金、エンタイトルメント。
無料化により不要になった（Q5・Q7）。最良の部品は存在しない部品である。

## 6. 免責

本アプリはエンターテインメントおよび内省の補助を目的とする。医療・法律・投資の
助言ではない。UIとAPIレスポンスの双方でこれを明示する（`disclaimer` フィールド）。
