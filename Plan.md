# Magi — 開発計画書

世界各地の占術を横断的に試せる、**完全無料**のWebアプリケーション。
コードはGitHubで公開する（MIT）。

---

## 0. 方法論

本計画は **ソクラテス式問答**（前提を問い直す）と **イーロン・マスク式思考法**
（第一原理思考 → 5ステップ・アルゴリズム）で導出する。実装は結論だけでなく、
「なぜその形なのか」を辿れる状態で残す。

---

## 1. ソクラテス式問答 — 前提の解体

**Q1. 利用者は「占い」に何を支払っているのか。当たる予言か？**
違う。当たり外れは検証不能で、検証されても継続の理由にならない。
支払われているのは **「意思決定と内省のための、外部から与えられる構造」** である。
毎日ひとつ、自分の外側から問いと象徴が渡され、それを解釈する時間が生まれる。

**Q2. ならば「当たること」は要件から外せるか？**
外せる。外すべきである。予言の的中を約束した瞬間、検証不能な主張の販売になり、
法的にも倫理的にも脆くなる。要件は「一貫性・再現性・解釈の豊かさ」に置き換わる。

**Q3. 一貫性とは何か。占いは乱数ではないのか？**
乱数だが、**同じ人が同じ日に同じ問いで引いた結果は同じでなければならない**。
朝引いたタロットが昼にリロードで変わるなら、それは占いではなくガチャである。
→ 乱数は「入力から決定的に導かれるシード」に置き換わる。
   `seed = SHA256(subject_key | engine_id | target_date | 正規化した問い)`

**Q4. なぜ「世界の色々な占い」でなければならないのか。ひとつで足りないか？**
足りない。単一占術は「もう見た」で飽きる。一方、複数の伝統は同じ問いに
**別々の象徴体系で答える** ため、比較そのものが体験価値になる（西洋タロットの「塔」と
易経の「震為雷」を並べる）。多流派は装飾ではなく、**本プロダクトが存在する理由**そのものである。

**Q5. では、いくらで売るべきか？**
**売らない。** 当初は月額サブスクを前提に設計していたが、これは前提から誤っていた。
課金する以上、「支払った人だけが得られるもの」を作らねばならない。しかし本アプリの提供物
（第2章の通り限界費用ほぼゼロの純関数と静的テキスト）で差をつけようとすると、
**わざと劣化させた無料版を作る**しかない。占術を減らす、カードを減らす、履歴を消す——
いずれも「利用者のための設計」ではなく「課金のための設計」であり、プロダクトを歪める。

さらに課金は、課金それ自体を支えるための巨大な付随物を要求する：ユーザー登録、認証、
決済事業者との連携、Webhook、契約状態の同期、解約導線、返金、税務。
**占いのコードより課金のコードの方が大きくなる。** 限界費用がゼロの商品に対して、
これは明らかに不合理な投資である。

→ 完全無料で公開する。中核体験「今日の三賢者（Daily Magi）」は、
毎日ログインさせるための仕掛けではなく、単に毎日価値が生まれる形だから採用する。

**Q6. 生成AIに毎回文章を書かせるべきか？**
すべきでない。第一原理で見ると、LLM推論は 1リクエストあたり数円のコストと数秒の遅延、
そして **非決定性**（Q3の要件違反）を持ち込む。占術の解釈テキストは有限個の
象徴（78枚・64卦・24ルーン…）の組み合わせであり、**あらかじめ書ける**。
→ v1のコアは純関数。LLMは将来「解釈の言い換え」に限定した任意機能とする。

**Q6-b. 無料なら、コードを公開する意味はどこにあるのか？**
**信頼**である。占いアプリに向けられる最大の疑念は「結果は操作されているのではないか」
（課金導線に有利な結果が出る、広告のために不安を煽る）。無料かつコード公開かつ
決定的シード（Q3）という3点が揃って初めて、**利用者は自分の結果を自分で再現・検証できる**。
これは既存の占いアプリがほぼ提供できていない性質であり、本プロダクトの唯一の差別化である。

**Q7. 無料は持続可能か。誰がサーバ代を払うのか？**
ここが本当に解くべき問題である。しかし第2章の分解を見れば、**サーバに状態を持つ理由が
そもそも無い**ことがわかる。占術は純関数、解釈は静的データ、鑑定結果はその場で計算できる。
ユーザーごとの状態は「過去に何を引いたか」だけであり、それは**利用者自身のブラウザに置けばよい**。
→ データベースを持たない。ログインを持たない。個人情報を収集しない。
結果として運用費は静的配信＋軽量APIのみとなり、無料枠のホスティングで賄える。
誰かの善意にも広告にも依存しない。**費用がほぼゼロだから無料が持続する。**

**Q8. フロントエンドに専用サーバとビルド工程は必要か？**
必要ない。サーバ状態もSSRすべき動的コンテンツも無いため、専用サーバとビルド工程は
ユーザーに見える価値を増やさない。→ 静的ファイルをFastAPIから配信し、self-hostは
1プロセスのままにする。npm依存とビルド工程、2つ目のデプロイ面を削除できる。

**Q9. self-hostにcomposeとオーケストレーションは必要か？**
必要ない。プロセスは1つ、バッキングサービスは0なので、composeを追加しても
`docker run` と等価なファイルを増やすだけである。代わりに価値があるのは、
受け取った鑑定を第三者が手元で再現・検証できることだ。composeではなく、
結果の再現検証CLIを追加する。

---

## 2. 第一原理からの再構築

物理的に必要なものだけに分解する。

| 要素 | 物理的実体 | 限界コスト |
|---|---|---|
| 占術の計算 | 決定的な純関数（表引き＋シード付きシャッフル） | ≒ 0 |
| 解釈テキスト | 静的データ（一度書けば無限に再利用） | 0 |
| 保存 | 1鑑定あたり数KBのJSON | ≒ 0 |
| 配信 | HTTPレスポンス | ≒ 0 |

**結論**: 限界費用がほぼゼロである。ならば利用者を絞る技術的理由は存在しない。
ゼロコストの商品にわざわざ計量・課金・制限の機構を足すことは、**費用を増やして価値を減らす**行為である。
→ 全機能を無条件に開放する。

### マスク式5ステップの適用

1. **要件を疑う** — 「占星術には天体暦(ephemeris)が要る」→ v1のホロスコープは
   太陽星座・月相・曜日支配星までで成立する。数百MBの暦データは v1 の要件ではない。
2. **部品を削除する** — 削除するもの: 会員同士のSNS、チャット、鑑定士マーケット、
   広告、ネイティブアプリ、LLM常時利用、リアルタイム通知基盤。
3. **単純化・最適化** — 全占術を `cast(input, rng) -> Reading` という **単一インタフェース**に
   統一する。流派の追加は「1ファイル追加」で終わる形にする（これが本プロダクトの成長率を決める）。
4. **サイクルタイムの短縮** — 占術エンジンはDB・ネットワーク・時刻に依存させない。
   純関数ゆえにテストはミリ秒で終わり、1日に何十回でも流派を追加できる。
5. **自動化は最後** — デプロイやコンテンツ生成の自動化は、流派が揃ってから。

---

## 3. プロダクト定義

### 中核体験
**今日の三賢者**: 毎朝、異なる伝統から3つの占術が自動で選ばれ、同じ問いに答える。
3つの解釈の「重なり」をアプリが要約する。毎日、3流派を引ける。

無料・ログイン不要・DBなしの帰結として、**個人情報を一切保存しない**。
生年月日や氏名は鑑定の計算にのみ使い、サーバ側には残さない（ログにも出さない）。

### 収録占術（v1: 8流派・すべて無料）

| ID | 名称 | 伝統 | 必要入力 | 象徴数 |
|---|---|---|---|---|
| `tarot` | タロット | 西洋 | なし（問いは任意） | 78枚 × 正逆 |
| `iching` | 易経（周易） | 中国 | なし | 64卦 × 変爻 |
| `runes` | ルーン | 北欧 | なし | エルダー・フサルク24 |
| `omikuji` | おみくじ | 日本 | なし | 7段階＋7項目 |
| `astrology` | 西洋占星術 | 西洋 | 生年月日 | 12星座＋月相 |
| `numerology` | 数秘術 | 西洋（ピタゴラス） | 氏名・生年月日 | 1-9, 11, 22, 33 |
| `bazi` | 干支・四柱推命（略式） | 中国 | 生年月日 | 十干十二支60 |
| `mayan` | マヤ暦ツォルキン | 中米 | 生年月日 | 260 kin |

### 提供範囲

8流派すべて、全スプレッド、今日の三賢者、無制限に無料。ログイン不要。
利用制限は **濫用防止のためのIPレートリミットのみ**（プラン差ではない）。
履歴は利用者のブラウザ（localStorage）に保存し、エクスポート・削除は利用者の手にある。

---

## 4. アーキテクチャ

技術スタックは kAIkei と揃える（同一開発者の学習コストを増やさない）。

- Backend: Python 3.12 / FastAPI / Pydantic v2（**ステートレス。DBなし・認証なし**）
- Frontend: HTML / CSS / ES Modules（FastAPIから静的配信、履歴はlocalStorage）
- CI: GitHub Actions（ruff + pytest）
- 配信: 静的フロント＋軽量API。誰でも `docker run` で self-host できる形を維持する。

```
backend/app/
├── divination/            # 占術ドメイン（DB・HTTP非依存の純粋層）
│   ├── base.py            # DivinationInput / Reading / DivinationEngine
│   ├── seed.py            # 決定的シードと SeededRandom
│   ├── registry.py        # エンジン登録・取得
│   ├── engines/           # 1流派 = 1ファイル
│   └── data/              # 象徴と解釈テキスト（静的JSON/Python）
├── api/v1/                # HTTP境界（ステートレス）
└── core/                  # 設定・レートリミット
```

### 中核インタフェース

```python
class DivinationInput(BaseModel):
    target_date: date                 # 占う対象日（既定=今日）
    question: str | None = None
    birth_date: date | None = None
    birth_time: time | None = None
    full_name: str | None = None
    options: dict[str, str] = {}      # spread など流派固有の指定

class DrawnSymbol(BaseModel):
    key: str                          # "the-tower" / "hex-51" / "fehu"
    name: str
    position: str                     # "過去" / "上卦" など
    reversed: bool = False
    image_hint: str | None = None

class Reading(BaseModel):
    engine_id: str
    engine_name: str
    tradition: str
    seed: str                         # 検証可能な形で必ず返す
    drawn: list[DrawnSymbol]
    summary: str
    sections: list[ReadingSection]    # 総合／恋愛／仕事／金運／助言
    score: int | None                 # 0-100
    lucky: LuckyItems | None
    generated_at: datetime

class DivinationEngine(Protocol):
    id: str; name: str; tradition: str
    required_fields: frozenset[str]
    def cast(self, inp: DivinationInput, rng: SeededRandom) -> Reading: ...
```

**不変条件**: `cast` は同一 (input, seed) に対して常に同一の `Reading` を返す。
これは全エンジン共通のテストで機械的に検証する。

---

## 5. ロードマップ

| フェーズ | 内容 | 状態 |
|---|---|---|
| P1 | 占術ドメイン層（8流派）＋ 決定的シード ＋ ステートレスAPI ＋ CI | 本PR |
| P2 | 静的フロントエンド（今日の三賢者・鑑定画面・履歴=localStorage・共有リンク） | 予定 |
| P3 | 流派拡張（ホラリー占星術・易の変卦詳細・カバラ・ジオマンシー・西洋以外のスプレッド） | 予定 |
| P4 | self-host 一式（Dockerfile・デプロイ手順）と結果の再現検証CLI | 完了 |
| P5 | LLMによる解釈の言い換え（任意機能・自前APIキー持ち込み制・決定的キャッシュ） | 予定 |

**削除した計画**: 認証、PostgreSQL、Stripe課金、エンタイトルメント。
無料化により不要になった（Q5・Q7）。最良の部品は存在しない部品である。

## 6. 免責

本アプリはエンターテインメントおよび内省の補助を目的とする。医療・法律・投資の
助言ではない。UIとAPIレスポンスの双方でこれを明示する（`disclaimer` フィールド）。
